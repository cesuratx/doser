use rstest::rstest;

#[rstest]
fn placeholder_noop() {
    // CSV header validation is covered in doser_config crate tests and CLI integration.
    // Intentionally empty test to keep module structure.
}
